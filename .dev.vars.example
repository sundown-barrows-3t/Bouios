# The bearer token that protects your memory gateway.
# During deploy, Cloudflare prompts you to set this as a secret.
# Generate a strong random value, for example:  openssl rand -hex 32
# Keep a copy — you paste it into your claude.ai connector URL as /mcp/<token>.
BEARER_TOKEN=
